export class Student{
 constructor(public studentId: number, public firstName: string, public lastName: string, public address: string,
          public emailId: string, public phoneNumber: number, public semester: number){}
   }
